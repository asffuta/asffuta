import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: "com.stacksail.asffuta",
  appName: "ASFFUTA Bible Study Outline",
  webDir: "build",
  bundledWebRuntime: true,
};

export default config;
